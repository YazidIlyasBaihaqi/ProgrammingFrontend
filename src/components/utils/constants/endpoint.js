const BASE_URL = `https://covid19.mathdro.id/api`;

const ENDPOINTS = {
    GLOBAL: `${BASE_URL}`,
    INDONESIA: `${BASE_URL}/countries/indonesia`,
    IMG: `${BASE_URL}/og`,
    IMGINDO: `${BASE_URL}/countries/indonesia/og`
}

export default ENDPOINTS;